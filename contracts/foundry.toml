[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.27"
optimizer = true
optimizer_runs = 200
via_ir = false
evm_version = "cancun"

[rpc_endpoints]
rh_testnet = "https://rpc.testnet.chain.robinhood.com"
arb_sepolia = "https://sepolia-rollup.arbitrum.io/rpc"

# Blockscout-backed verifier handled in deploy scripts.
